# it is similar to the bytes
# index starting from 0
# byte array can be modified

ele = [10, 20, 30, 40, 50, 60]
x = bytearray(ele)

# update/ replace the element
x[1] = 200

# print the index
print(x[1])
