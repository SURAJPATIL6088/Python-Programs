# it is generally to use the image pixel
# we can store ony numbers 0 to 123..
# can't store string value
# can't store the negative values
# byte can not be modified

ele = [10, 20, 30, 40, 50, 60]

x = bytes(ele)

# data-type of ele
print(type(ele))

# data-type of x
print(type(x))
